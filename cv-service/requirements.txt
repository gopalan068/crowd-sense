# CV Service dependencies
# Pin major versions; patch updates are safe.

# YOLOv8 person detection (includes torch as a transitive dep)
ultralytics>=8.0.0,<9.0.0

# Video capture and frame processing
# Use opencv-python-headless (no GUI) for server/Docker environments.
# For local dev where you want to display frames, swap to opencv-python.
opencv-python-headless>=4.8.0,<5.0.0

# HTTP POST to backend
requests>=2.31.0,<3.0.0

# .env file loading
python-dotenv>=1.0.0,<2.0.0
